<div class="flip-card">
	<div class="card-front" id="card1-front">
	  <img src="<%= Acheivement.find(acheivement.achievement_id).image %>" class="logo1" />
	</div>
	<div class="card-back" id="card1-back">
	  <div class="bar"></div>
	  <p class="text"><%= Acheivement.find(acheivement.achievement_id).name %>
	  </p>
    <p class="text"><%= Acheivement.find(acheivement.achievement_id).dec %>
    </p>
	</div>
</div>