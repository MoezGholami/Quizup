
<div class="courses_box1">
  <div class="container">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {:class => 'login', method: :post }) do |f| %>
        <%= devise_error_messages! %>

        <p align="center">
          اگر كلمه عبور خود را فراموش كرده اید می توانید با وارد كردن ادرس ایمیلی كه در موقع ثبت نام وارد كرده اید كلمه عبور خود را از طریق ایمیل دریافت كنید .
        </p>

        <div class="field">
         <br>
          <%= f.email_field :email, autofocus: true,:placeholder => "ایمیل خود را اینجا وارد کنید", :class => 'required form-control' %>
        </div>


        <div class="actions">
          <%= f.submit "ارسال", :class => "btn btn-primary btn-lg1 btn-block" %>
        </div>
        <%= render "devise/shared/links" %>

  <% end %>

  </div>
</div>
